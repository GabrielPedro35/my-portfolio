# Evolution

## Last failures
- `pnpm dev` failed with ERR_PNPM_IGNORED_BUILDS: esbuild, sharp, unrs-resolver. Cause: `pnpm-workspace.yaml` `allowBuilds` left as placeholders ("set this to true or false") instead of `true`.
